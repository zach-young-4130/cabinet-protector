{
  "installCommand": "npm install && npm install --prefix server",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/protect-vinyl/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
